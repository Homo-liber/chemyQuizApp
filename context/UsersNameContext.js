import { createContext } from "react";

// should be named like your file (best practice), creates an object that has a property
//called provider, which we will use later
export const UsersNameContext = createContext();
